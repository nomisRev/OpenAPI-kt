# Module OpenAPI Kotlin Parser

Kotlin Multiplatform library for parsing OpenAPI 3.x specifications into a typesafe Kotlin model.

Supports JSON (`kotlinx-serialization-json`) and YAML (`kaml`) input and runs on JVM, macOS (Arm64), and JS/IR.
